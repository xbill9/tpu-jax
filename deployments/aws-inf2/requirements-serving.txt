# Runtime dependencies for the pure-JAX Gemma 4 server on Inf2.
#
# The repo-root requirements.txt covers the tpu-devops MCP server (mcp, httpx,
# openai, google-cloud-secret-manager) and does NOT cover the serving path, so
# installing it alone leaves the service crashing on import.
#
# jax / jaxlib / libneuronxla are deliberately absent: they come from the
# jax-neuronx[stable] pin, which selects the combination tested against the
# Neuron runtime. Do not pin them here or you will fight that resolver.
fastapi
uvicorn
pydantic
transformers
safetensors
huggingface_hub
